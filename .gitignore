venv/
*.pyc
__pycache__/
.env
.vscode/
.idea/
eval/results.md
# (Unless the user specifically wants to commit the results, but usually results are generated)
# Wait, the instruction says: "The repository must include the following artifacts at the root level: ... A directory named eval/".
# So I SHOULD commit eval/ and the results.md if it needs to be there for the submission.
# I'll keep results.md for now since it's a requirement.
